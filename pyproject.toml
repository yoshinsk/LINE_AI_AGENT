[project]
name = "line-ai-agent"
version = "0.1.0"
description = "LINE webhook ingress and Windows Codex worker for NSK AI agent."
requires-python = ">=3.11"
dependencies = []

[tool.unittest]
start-dir = "tests"

