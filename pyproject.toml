[project]
name = "line-ai-agent"
version = "0.1.0"
description = "LINE webhook ingress and Codex worker for an AI agent."
requires-python = ">=3.11"
dependencies = [
    "openpyxl>=3.1,<4",
    "pymupdf>=1.27,<2",
    "python-pptx>=1.0,<2",
    "python-docx>=1.1,<2",
]
license = "MIT"

[tool.unittest]
start-dir = "tests"
