[project]
name = "line-ai-agent"
version = "0.1.0"
description = "LINE webhook ingress and Codex worker for an AI agent."
requires-python = ">=3.11"
dependencies = []
license = "MIT"

[tool.unittest]
start-dir = "tests"
